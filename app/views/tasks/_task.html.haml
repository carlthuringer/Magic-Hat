%li#task_complete.task{ :id => task.id, :class => task.active? ? nil : 'task-active' }
  = link_to task.description, complete_toggle_task_path(task),
    'data-ajax' => 'false', :remote => true, :method => :put
  = link_to "Edit", edit_task_path(task)
