%h1 Sign in

= form_for(:session, :url => sessions_path) do |f|
  %div.field
    = f.label :email
    = f.text_field :email
  %div.field
    = f.label :password
    = f.password_field :password
  %div.actions
    = f.submit "Sign in"

%p
  New User? 
  = link_to "Sign up now!", signup_path
